#[
    #! INSIDE LIBRARIES
    
    Dragonstone Standard Library (Native-only extensions).

    Import this file in addition to `stdlib` if you need interpreter-only
    modules (e.g., utilities backed by Crystal FFI bindings).
]#

use "./modules/native/**/module"
