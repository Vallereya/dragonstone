class StringIO
    def initialize(@string)
        @pos = 0
    end

    def read_char
        if @pos >= @string.length
            return nil 
        end

        char = @string[@pos]
        @pos += 1
        char
    end
end
