module Dragonstone
    module Lib
        module Crystal
            module Kernel
                
                # Wraps the Crystal puts function.
                def puts(args)
                    ffi.call_crystal("puts", [args])
                end
            end
        end
    end
end

Dragonstone::Lib::Crystal::Kernel.puts("Hello World!")