# Example One
greet = ->(name: str) { "Hello, #{name}!" }

puts greet.call("Alice")  # Outputs: Hello, Alice!

# Example Two
square: para(int, int) = ->(x: int) { x * x }
puts square.call(6)  # Outputs: 36

