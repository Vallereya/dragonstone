def divide(a: int, b: int)
    if b == 0
        raise "Cannot divide by zero"
    end
    a / b
end

echo divide(10, 2)  # Outecho: 5

begin
    echo divide(10, 0)
rescue ex
    echo "Caught an exception: #{ex.message}"
end
