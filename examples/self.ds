#[
    Self Example
]#

class Person
    property name: str

    def initialize(name: str)
        # Using self to access the setter method.
        self.name = name
    end

    def greet
        # Using self to access the property
        echo "Hello, my name is #{self.name}"
    end
end

person = Person.new("Jules")
person.greet
