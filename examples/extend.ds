# Module with helper methods to extend into other types
module Logger
    def hello(name: str)
        "Hello, #{name}!"
    end
end

# Classes can copy module methods into their eigenclass with extend
class Greeter
    extend Logger
end

echo Greeter.hello("world")
