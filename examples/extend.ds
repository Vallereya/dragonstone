#[
    Extend Example
]#

# Module with a helper to extend into other types.
module Logger
    def hello(name: str)
        "Hello, #{name}!"
    end
end

# Classes can copy module methods into their class with extend.
class Greeter
    extend Logger
end

echo Greeter.hello("world")
