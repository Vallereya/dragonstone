#[
    Ternary Operator Example
]#

age = 20
status = age >= 18 ? "Adult" : "Minor"
echo "Status: #{status}"

# Nested ternary
score = 85
grade = score > 90 ? "A" : (score > 80 ? "B" : "C")
echo "Grade: #{grade}"
